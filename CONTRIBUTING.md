# Contributing

-   Usa Conventional Commits: feat, fix, chore, docs, style, refactor, perf, test
-   Ejecuta `npm run lint` y `npm run build` antes de abrir un PR
-   Añade tests para cambios de lógica (jest/ react-testing-library)
